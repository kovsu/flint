---
import { PackageManagers } from "starlight-package-managers";
import FlankedLinkButton from "./FlankedLinkButton.astro";
---

<div class="try-it-out" id="try-it-out">
	<h2>Try out Flint on any web project:</h2>

	<PackageManagers type="dlx" pkg="flint" />

	<div class="ad-area">
		<p>
			Frustrated with slow linting that doesn't catch what you actually care
			about?
		</p>
		<p>
			Or maybe your linter is fast, but doesn't include powerful lint rules?
		</p>
		<p class="ad-last-line">Let Flint help you out.</p>
	</div>
	<FlankedLinkButton href="/about">
		Get started linting with Flint
	</FlankedLinkButton>
</div>

<style>
	.try-it-out {
		display: flex;
		flex-direction: column;
		margin: auto;
		max-width: 49rem;
		padding: 6rem 0 4rem;
	}

	h2,
	p {
		text-align: center;
	}

	h2 {
		margin-bottom: 2rem;
	}

	p {
		font-size: var(--sl-text-xl);
		line-height: 1.25;
		margin: 0;
	}

	.ad-area {
		padding: 2rem 0;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.ad-last-line {
		margin-top: 1rem;
	}

	.get-started-area {
		align-items: center;
		display: flex;
		flex-direction: row;
		gap: 2rem;
		margin-top: 4rem;
	}

	a {
		margin: 0;
		margin-inline-end: 0;
	}

	.hr {
		background: var(--sl-color-gray-5);
		display: block;
		height: 2px;
		width: 100%;
	}

	.get-started-button-text {
		display: block;
		font-size: var(--sl-text-xl);
		margin: 0 1rem;
		text-wrap: nowrap;
	}
</style>
