---
description: "Enforce that the `exports` property is present."
title: "exportsPresence"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="exportsPresence" />

npm requires that the `exports` field in `package.json` is present.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{}
```

```json
{
	"name": "my-repository"
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"exports": {}
}
```

```json
{
	"exports": {},
	"name": "my-repository"
}
```

</TabItem>
</Tabs>

## Options

### `ignorePrivate`

Whether the property should still be required when the package's `private` property is `true`.
Defaults to `false`.

This option can be useful if the rule is enabled on many `package.json` files and some files differ in privacy.

## When Not To Use It

If your package doesn't need to specify `exports`, you can disable this rule.

## Related Rules

- [exportsValidity](/rules/package-json/exportsvalidity) - Enforces that the `exports` property is valid.

## Further Reading

- [npm: `package.json` > `exports`](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#exports)

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="exportsPresence" />
