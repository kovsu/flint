---
description: "Enforce that the `main` property is valid."
title: "mainValidity"
topic: "rules"
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { RuleEquivalents } from "~/components/RuleEquivalents";
import RuleSummary from "~/components/RuleSummary.astro";

<RuleSummary plugin="package-json" rule="mainValidity" />

This rule checks that the `main` property is a non-empty string.

## Examples

<Tabs>
<TabItem label="❌ Incorrect">

```json
{
	"main": ["index.js", "secondary.js"]
}
```

</TabItem>
<TabItem label="✅ Correct">

```json
{
	"main": "index.js"
}
```

</TabItem>
</Tabs>

## When Not To Use It

If you don't need compliance with npm's `main` field specification, you can disable this rule.
For example, if you use a different package manager that treats the field differently, this rule may not be correct for you.

## Related Rules

- [mainPresence](/rules/package-json/mainpresence) - Enforces that the `main` property is present.

## Further Reading

- [npm: `package.json` > `main`](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#main)

## Equivalents in Other Linters

<RuleEquivalents pluginId="package-json" ruleId="mainValidity" />
